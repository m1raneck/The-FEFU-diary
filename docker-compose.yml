networks:
  diary_network:
    driver: bridge

services:
  postgres:
    image: postgres:15
    container_name: fefu-postgres
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-user}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-password}
      POSTGRES_DB: ${POSTGRES_DB:-university_db}
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./database/01_init.sql:/docker-entrypoint-initdb.d/01_init.sql
      - ./database/02_seed.sql:/docker-entrypoint-initdb.d/02_seed.sql
      - ./database/03_students.sql:/docker-entrypoint-initdb.d/03_students.sql
      - ./database/04_grade_scaling.sql:/docker-entrypoint-initdb.d/04_grade_scaling.sql
      - ./database/05_grade_column_settings.sql:/docker-entrypoint-initdb.d/05_grade_column_settings.sql
      - ./database/06_lesson_comments.sql:/docker-entrypoint-initdb.d/06_lesson_comments.sql
    ports:
      - "5432:5432"
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-user} -d ${POSTGRES_DB:-university_db}"]
      interval: 5s
      timeout: 5s
      retries: 5
    networks:
      - diary_network

  auth-service:
    build:
      context: .
      dockerfile: ./services/auth-service/Dockerfile
    ports:
      - "8000:8000"
    env_file:
      - path: config/.env
        required: false
    environment:
      DATABASE_URL: ${DATABASE_URL:-postgresql://user:password@postgres:5432/university_db}
      SECRET_KEY: ${SECRET_KEY:-change-me-in-production}
    depends_on:
      postgres:
        condition: service_healthy
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request; urllib.request.urlopen('http://localhost:8000/health')\""]
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 10s
    networks:
      - diary_network

  attendance-service:
    build:
      context: .
      dockerfile: ./services/attendance-service/Dockerfile
    ports:
      - "8001:8000"
    env_file:
      - path: config/.env
        required: false
    environment:
      DATABASE_URL: ${DATABASE_URL:-postgresql://user:password@postgres:5432/university_db}
      SECRET_KEY: ${SECRET_KEY:-change-me-in-production}
    depends_on:
      postgres:
        condition: service_healthy
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request; urllib.request.urlopen('http://localhost:8000/health')\""]
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 10s
    networks:
      - diary_network

  grades-service:
    build:
      context: .
      dockerfile: ./services/grades-service/Dockerfile
    ports:
      - "8002:8000"
    env_file:
      - path: config/.env
        required: false
    environment:
      DATABASE_URL: ${DATABASE_URL:-postgresql://user:password@postgres:5432/university_db}
      SECRET_KEY: ${SECRET_KEY:-change-me-in-production}
    depends_on:
      postgres:
        condition: service_healthy
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request; urllib.request.urlopen('http://localhost:8000/health')\""]
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 10s
    networks:
      - diary_network

  schedule-service:
    build:
      context: .
      dockerfile: ./services/schedule-service/Dockerfile
    ports:
      - "8003:8000"
    env_file:
      - path: config/.env
        required: false
    environment:
      DATABASE_URL: ${DATABASE_URL:-postgresql://user:password@postgres:5432/university_db}
      SECRET_KEY: ${SECRET_KEY:-change-me-in-production}
    depends_on:
      postgres:
        condition: service_healthy
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request; urllib.request.urlopen('http://localhost:8000/health')\""]
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 10s
    networks:
      - diary_network

  gateway:
    image: nginx:alpine
    ports:
      - "80:80"
    volumes:
      - ./config/nginx.conf:/etc/nginx/nginx.conf
    depends_on:
      auth-service:
        condition: service_healthy
      attendance-service:
        condition: service_healthy
      grades-service:
        condition: service_healthy
      schedule-service:
        condition: service_healthy
    restart: unless-stopped
    networks:
      - diary_network

  frontend:
    build: ./frontend
    ports:
      - "8080:80"
    depends_on:
      - gateway
    restart: unless-stopped
    networks:
      - diary_network

volumes:
  postgres_data:
