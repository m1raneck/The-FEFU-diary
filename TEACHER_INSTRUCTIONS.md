# Инструкция по использованию системы

## Данные для входа

### Преподаватель
- **Почта:** derbentcev.no@dvfu.ru
- **Пароль:** 123

### Студент (для тестирования)
- **Почта:** petrov@student.ru
- **Пароль:** (любой пароль с хешем hash_student_123)

## Функциональность преподавателя

После входа с учетными данными преподавателя вы увидите:

1. **Расписание преподавателя** на неделю
   - Вторник, 10:10 - Группа Б9124-09.03.03ру (31 чел.), Проектная деятельность, D547
   - Вторник, 11:50 - Группа Б9125-09.03.04ру (26 чел.), Проектная деятельность, D740
   - Суббота, 10:10 - Группа Б9125-02.03.01мо (25 чел.), Проектная деятельность, D945

2. **Выставление оценок**
   - Кликните на пару в расписании
   - Откроется модальное окно с журналом оценок
   - Можно выставлять оценки студентам (2-5)
   - Можно добавлять комментарии
   - Можно отмечать присутствие/отсутствие
   - Можно импортировать оценки из CSV файла

## Импорт оценок

1. В модальном окне журнала кликните "Загрузить CSV"
2. Подготовьте CSV файл с колонками: Студент, Тест 1 (%), Тест 2 (%)
3. Загрузите файл
4. Нажмите "➕ Импорт нескольких тестов"
5. Установите соответствие колонок CSV датам
6. Нажмите "📥 Выставить оценки"

## Данные в системе

### Группы студентов
- **Б9124-09.03.03ру** (31 студент)
- **Б9125-09.03.04ру** (26 студентов)
- **Б9125-02.03.01мо** (25 студентов)

Итого: 82 студента с автоматически сгенерированными именами и фамилиями.

### Предмет
- **Проектная деятельность** (ПД)

## Развертывание

1. Убедитесь что Docker и Docker Compose установлены
2. Перейдите в директорию проекта:
   ```bash
   cd The-FEFU-diary
   ```
3. Запустите приложение:
   ```bash
   docker-compose up -d
   ```
4. Ождите пока все сервисы запустятся (проверьте логи если есть ошибки)
5. Откройте в браузере: http://localhost:8080

## Проверка БД

Если нужно проверить данные в БД:
```bash
docker exec -it university psql -U user -d university_db
```

Примеры запросов:
```sql
-- Показать всех преподавателей
SELECT u.full_name, t.department FROM teachers t JOIN users u ON t.user_id = u.id;

-- Показать расписание преподавателя Дербенцева
SELECT g.name, s.name, r.number, sch.weekday, sch.lesson_number 
FROM schedule sch
JOIN groups g ON sch.group_id = g.id
JOIN subjects s ON sch.subject_id = s.id
JOIN teachers t ON sch.teacher_id = t.id
JOIN users u ON t.user_id = u.id
LEFT JOIN rooms r ON sch.room_id = r.id
WHERE u.email = 'derbentcev.no@dvfu.ru';

-- Показать студентов группы Б9124-09.03.03ру
SELECT u.full_name FROM students s 
JOIN users u ON s.user_id = u.id 
JOIN groups g ON s.group_id = g.id
WHERE g.name = 'Б9124-09.03.03ру';
```
